# Gradle properties
org.gradle.jvmargs=-Xmx4g
org.gradle.parallel=true
org.gradle.caching=true

# Kotlin configuration
kotlin.code.style=official
kotlin.js.generate.executable.default=false

# Disable browser-related tasks for Foundry VTT module
# This is a Foundry module, not a standalone web app
kotlin.js.browser.run.disabled=true
kotlin.js.browser.test.disabled=true

# Module deployment path (update this to your Foundry modules directory)
# foundry.modulesDir=/Users/mark/Library/Application Support/FoundryVTT/Data/modules
