package at.kmlite.pfrpg2e.kingdom

/**
 * The legacy chat context menus depended on the RP/Ruin system. They’re removed
 * while the Reignmaker-lite actions are rebuilt, so registration is a no-op.
 */
fun registerContextMenus() {
    // No kingdom-specific chat context actions at the moment.
}
